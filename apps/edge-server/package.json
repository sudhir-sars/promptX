{
	"name": "promptx-edge-server",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"build": "wrangler deploy --dry-run --outdir dist",
		"type-check": "tsc --noEmit",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"@promptx/shared": "workspace:*",
		"hono": "^4.6.0"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^25.9.3",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.100.0"
	}
}
